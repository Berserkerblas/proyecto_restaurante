doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="vistaprincipal", content="width=device-width, initial-scale=1.0")
        title #{nombrePagina} | #{pagina}
        // Link en pug para mis fuentes:
        link(rel='preconnect' href='https://fonts.googleapis.com')
        link(rel='preconnect' href='https://fonts.gstatic.com' crossorigin='')
        // Fuente de los h1, h2...:
        link(href='https://fonts.googleapis.com/css2?family=Staatliches&display=swap' rel='stylesheet')
        // Fuente de los p, li, a:
        link(href='https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap' rel='stylesheet')
        // Fuente h4:
        link(href='https://fonts.googleapis.com/css2?family=Imperial+Script&display=swap' rel='stylesheet')
        // Así es como linqueo mi css, como esta dentro del head del header se aplicará a todas las vistas que lleven este header:
        link(rel="stylesheet", href="/css/styles.css")
        // Linkeo de bootsrtrap:
        link(
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
        )
        // Linkeo con font awesome, para los emoticonos y demás:
        link(
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        )
        // Linkeo de flatpickr para el calendario de las reservas
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css")

        // Script de BootstrapBundle para que vaya el corousel:
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")
    body
        include header
        block contenido
        include footer